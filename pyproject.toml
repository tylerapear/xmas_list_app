[project]
name = "giftify"
version = "0.0.1"
readme = "README.md"
requires-python = ">=3.12.3"
authors = [
    {name = "Tyler Pearson", email = "tyler.a.pear@gmail.com"}
]
keywords = ["gifts", "wishlist", "registry", "christmas"]

dependencies = [
    "django >= 2.28.0",
    "coolname >= 4.2.0"
]